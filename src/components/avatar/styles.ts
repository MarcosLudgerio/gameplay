import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: 62,
        height: 62,
        borderRadius: 12,
        alignItems: 'center',
        justifyContent: 'center',
        marginRight: 22
    },
    avatar: {
        width: 58,
        height: 58,
        borderRadius: 12
    }
});